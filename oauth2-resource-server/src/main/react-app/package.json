{
  "name": "react-app",
  "version": "0.1.0",
  "private": true,
  "homepage": "http://localhost:5051/react/",
  "dependencies": {
    "hookrouter": "^1.2.3",
    "js-cookie": "^2.2.0",
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "react-scripts": "3.0.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "cross-env PUBLIC_URL=/react/ NODE_ENV=production react-scripts build",
    "deploy": "yarn install && yarn build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "cross-env": "^5.2.0",
    "shx": "^0.3.2"
  }
}
